# Objective Task Runtime

Task schemas describe the pieces of an objective. They are not actions and do not run gameplay.

Tasks require stable `taskId` values. Duplicate task ids inside one objective reject.
