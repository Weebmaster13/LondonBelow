# Objective Self-Checks

Objective self-checks are destructive and should run before runtime start.

They prove malformed schemas reject, duplicate objective/task/requirement/dependency ids reject, valid objectives register, malformed and unknown progress rejects, valid progress records, unsafe metadata/context/tags reject, forbidden execution fields reject, serialization rejects unsafe values, snapshots are isolated, diagnostics are read-only, histories are bounded, shutdown clears state, and no objective completion execution, quest execution, gameplay execution, UI, Workspace mutation, remotes, client authority, Save persistence, Narrative ownership, Horror pacing ownership, or Chapter content exists.
