# Puzzle Diagnostics

Puzzle diagnostics expose initialized, started, lifecycle state, puzzle count, node count, graph count, dependency count, condition count, validation failure count, snapshot count, runtime limits, serialization posture, snapshot isolation proof, last self-check result, and health state.

Diagnostics are read-only returned copies.

Validation failure payloads are sanitized before they appear in diagnostics.
