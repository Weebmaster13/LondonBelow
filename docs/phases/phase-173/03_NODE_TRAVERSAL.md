# Node Traversal

Node traversal validates the current node, destination node, execution state, and traversal history before advancing.

Supported node execution types:

- Text
- Choice
- Condition
- Variable
- Event Metadata
- End

Node traversal never renders text, plays audio, animates characters, mutates Workspace, executes commands, publishes events, or evaluates queries.
