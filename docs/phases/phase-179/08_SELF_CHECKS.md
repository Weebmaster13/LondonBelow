# Self Checks

Phase 179 self-checks verify runtime registration, duplicate runtime rejection, renderer registration, duplicate and invalid renderer rejection, request intake, session creation, duplicate session rejection, invalid request rejection, deterministic assignment ordering, lifecycle transitions, illegal lifecycle rejection, acknowledgement production, duplicate acknowledgement rejection, acknowledgement ownership, synchronization resolution, diagnostics isolation, snapshot isolation, evidence, metrics, profiler metadata, budgets, governance, certification posture, validation, reset, shutdown, and prohibited responsibility posture.

Automation command: `npm run london:phase179:selfcheck`.
