# Runtime Capability

`RenderingRuntimeCapabilityRegistry` registers the default runtime capability metadata with owner, domain, authority, dependencies, phase, version, provider, runtime, and certification posture.

Duplicate runtime registration is rejected and recorded as a structured runtime failure.
