# Self-Checks

Phase 165 self-checks cover command definitions, single-owner validation, requester registration, client authority rejection, handler registration, duplicate rejection, command submission validation, payload validation, priority ordering, FIFO ordering, idempotency protection, cancellation, diagnostics, snapshots, shutdown rejection, forbidden-surface absence, Bootstrap ordering, Governance, and package scripts.

Part II expands coverage for immutable command envelopes, lifecycle state machine snapshots, handler-before-admission posture, normalized command results, required identity fields, queue admission metadata, scheduling before execution, and standardized failure taxonomy.
