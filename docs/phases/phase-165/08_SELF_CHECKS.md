# Self-Checks

Phase 165 self-checks cover command definitions, single-owner validation, requester registration, client authority rejection, handler registration, duplicate rejection, command submission validation, payload validation, priority ordering, FIFO ordering, idempotency protection, cancellation, diagnostics, snapshots, shutdown rejection, forbidden-surface absence, Bootstrap ordering, Governance, and package scripts.

Part II expands coverage for immutable command envelopes, lifecycle state machine snapshots, handler-before-admission posture, normalized command results, required identity fields, queue admission metadata, scheduling before execution, and standardized failure taxonomy.

Part III expands coverage for execution policy validation, retry policy validation, lock id validation, bounded timeout enforcement, transaction commit and rollback metadata, lock acquisition and release cleanup, replay metadata, interrupted recovery metadata, batch metadata, nested command ancestry, circular submission rejection, maximum nesting depth rejection, diagnostics expansion, snapshot expansion, and forbidden-surface absence.

Part IV expands coverage for timeline recording, stage duration recording, execution graph generation, correlation graph generation, runtime health calculation, queue metrics, execution metrics, retry metrics, lock metrics, transaction health, replay health, profiler generation, runtime inspection, latency histograms, throughput metrics, pressure metrics, immutable diagnostics, immutable snapshots, and passive instrumentation boundaries.
