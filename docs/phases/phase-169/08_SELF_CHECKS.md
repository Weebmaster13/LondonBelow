# Self Checks

Phase 169 expands workflow self-checks for:

- lowerCamelCase provider consistency;
- integrated workflow activation;
- duplicate correlation rejection before mutation;
- message routing by correlation;
- missing correlation rejection;
- suspension records;
- resumption records;
- completion validation;
- integration diagnostics;
- integration snapshots;
- direct command execution absence;
- direct event publication absence;
- direct query execution absence;
- scheduler admission evidence;
- shutdown rejection.

The Node wrapper validates required files, source tokens, documentation presence, governance synchronization, package scripts, and banned runtime surfaces.
