# Street Audio Timeline

The opening street is staged as four deterministic segments:

- `ordinary_london`
- `house_reveal`
- `gate_approach`
- `boundary_crossing`

The runtime publishes attributes for the active segment and authored street event. Client UI may display captions and visual equivalents, but it does not own audio truth or progression.

Actual Sound instance creation remains blocked until final asset approval.
