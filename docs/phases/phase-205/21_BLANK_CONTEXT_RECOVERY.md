# Blank Context Recovery

Resume from repository state, not memory.

Required checks:

- Confirm HEAD and `origin/main`.
- Read `ROADMAP.md`, `TASKS.md`, `LONDON_ENGINE.md`, `LONDON_ENGINE_MASTER_CONTEXT.md`, and `automation/state/phase-state.json`.
- Inspect `docs/phases/phase-205`.
- Run `npm run london:phase205:selfcheck`.
- Keep Studio evidence blocked unless authoritative evidence exists.

Do not restore old GUI Phase 196 direction.
