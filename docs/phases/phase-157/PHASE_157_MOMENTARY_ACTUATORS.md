# Momentary Actuators

Momentary actuators support `ACTIVATE` from `READY` to `COOLDOWN`. Dependency metadata is validated as data, not arbitrary code.

The family does not introduce a scripting language or callback registry.
