# Diagnostics

Diagnostics expose `environmentalInteractionRuntimePosture`, family/object counts, counters, bounded failures, dependency count, and lowerCamelCase safety posture.

Diagnostics are read-only and bounded.
