# Object Identity

Each object has `id`, `interactionTargetId`, family, supported action ids, optional authored instance id, optional chapter id, and runtime registration revision.

Identity does not rely on Roblox instance names, display text, paths, or client-provided Instances.
