# Runtime Smoke Test

Run:

```powershell
npm run london:environmental-interactions
```

This command uses the Runtime Execution Framework. If authoritative Studio evidence is unavailable, the status is `executionBlocked`.
