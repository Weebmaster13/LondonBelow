# Security Review

The runtime rejects malformed definitions, unsafe metadata, duplicate ids, unsupported actions, invalid transitions, missing objects, repeat inspection violations, and Phase 156 authorization failures.

Clients cannot submit authoritative next state, physical position, handler names, or dependency execution code.
