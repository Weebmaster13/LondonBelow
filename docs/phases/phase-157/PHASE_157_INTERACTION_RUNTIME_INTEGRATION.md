# Interaction Runtime Integration

Environmental registration uses `InteractionCoordinator.registerTarget` and `InteractionCoordinator.registerInteraction`.

Environmental action execution uses `InteractionCoordinator.requestInteraction`; no direct client mutation path exists.
