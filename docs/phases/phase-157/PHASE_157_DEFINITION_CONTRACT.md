# Definition Contract

Definitions are validated by `EnvironmentalDefinitionValidation`. Invalid ids, unknown families, unsupported actions, invalid states, oversized metadata, unsafe runtime values, Roblox Instances, and malformed dependency fields reject before registration.

Definitions are copied into state and treated as immutable after registration.
