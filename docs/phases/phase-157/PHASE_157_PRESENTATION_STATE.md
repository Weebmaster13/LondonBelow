# Presentation State

Presentation state is derived from authoritative environmental state and includes safe keys such as prompt, animation state, audio state, visual state, state revision, enabled, and busy.

It does not expose server-only predicates, handler internals, stack traces, or authorization state.
