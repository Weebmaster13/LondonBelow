# Environmental Model

Environmental objects are immutable definitions plus authoritative runtime state. Definitions describe identity, family, supported actions, initial state, policies, metadata, presentation keys, and optional dependency data.

State transitions are deterministic and keyed by stable ids.
