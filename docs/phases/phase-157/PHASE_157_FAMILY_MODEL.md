# Family Model

Families expose narrow `validateDefinition` and `evaluate` functions. The runtime registers:

- `BinaryMechanism`
- `InspectableObject`
- `MomentaryActuator`

Families do not own networking or global request authorization.
