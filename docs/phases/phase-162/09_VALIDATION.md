# Validation

Phase 162 validates provider records, request records, response records, operations, provider kinds, retry modes, serializability, payload bounds, unsafe fields, and missing required fields.

Validation happens before provider execution. Failed validation does not mutate provider state.
