# Opening Ten Minutes

Implemented source-side improvements:

- Opening control and goal budgets are encoded in `BlackwaterQualityStrikeConfig`.
- Spawn street now has visible district history: newsstand, wheel rut, washline, boot marks, and gate chain.
- The first optional discovery asks why every witness vanished before Blackwater House.
- Lantern stability starts as a readable resource value.

Measured timing remains blocked until Studio execution.
