# Visual Feedback

Supported visual feedback command types:
- `HighlightObject`
- `RemoveHighlight`
- prompt visibility changes
- busy indicators
- success/denial metadata

Visual feedback is metadata only. It does not create Highlight instances, VFX, particles, UI instances, or Workspace mutation.
