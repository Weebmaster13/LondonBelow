# Dispatcher

The dispatcher records where a command would route:
- UI
- Audio
- System animation adapters
- VFX metadata adapters
- Accessibility

It owns no gameplay and performs no client delivery, audio playback, animation playback, remote creation, or Workspace mutation.
