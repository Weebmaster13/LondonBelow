# Participants, Variables, And Conditions

Supported participant types:

- Player
- NPC
- Group
- System

Variables belong to a conversation instance and never mutate unrelated capabilities.

Conditions are metadata-only in Phase 172. They may describe variable, workflow, or capability-interface checks, but they do not execute gameplay, query handlers, command handlers, inventory logic, AI behavior, or persistence.
