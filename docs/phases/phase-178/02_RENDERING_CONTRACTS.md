# Rendering Contracts

`RenderingContractRegistry` registers the default immutable contract metadata for Phase 178.

The default contract records owner, domain, authority, dependencies, provider identity, snapshot provider, diagnostics provider, phase, version, and certification status.

Duplicate default contract registration is rejected and recorded through diagnostics/evidence.
