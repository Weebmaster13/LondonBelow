# Phase 189 - Studio Certification Matrix

The strict importer requires 26 named passing cases: four input devices; disabled, unknown, callback, and reentrant failures; stale generation; allowed and rejected rapid revisions; connection balance; modal containment and priority; initial focus; PreserveOnly and Never preferences; polite/assertive live regions; preference rejection; PlayerGui remount and respawn recovery; unmount/shutdown cleanup; multiplayer isolation; and low-end budget.

Evidence must specify schema 1, exact Phase 189, RobloxStudio, authoritative and passed flags, a non-empty run ID, and every required test identity.

## Ownership

Phase 189 owns strict evidence acceptance and rejection.

## Non-Ownership

It does not own Studio availability or manufacture test output.

## Certification Boundary

Missing, partial, malformed, or failed evidence remains `executionBlocked`.
