# Diagnostics And Snapshots

Provider: `presentationRuntimeExecution`

Diagnostics expose scheduler state, execution queue state, executing sessions, acknowledgements, synchronization records, failures, evidence, metrics, profiler records, governance, certification, and runtime posture.

Snapshots are isolated deep copies under `presentationRuntimeExecutionSnapshot`.
