# Acknowledgements And Synchronization

`AcknowledgementExecutionEngine` produces deterministic metadata acknowledgements.

`SynchronizationExecutionEngine` evaluates acknowledgement and execution state and returns metadata decisions.

Synchronization never advances Dialogue Execution and never calls rendering, networking, or remotes.
