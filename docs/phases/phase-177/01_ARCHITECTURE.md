# Phase 177 Architecture

`PresentationExecutionCoordinator` registers after `PresentationRuntimeCoordinator`.

The runtime position is:

Dialogue Presentation Contract -> Presentation Runtime Capability -> Presentation Runtime Execution -> future rendering contracts.

Execution manages presentation state only. Rendering remains a future runtime.
