# Health Metrics Budgets

Capabilities publish health-only metadata:

- health;
- readiness;
- availability;
- dependency status through dependency graph diagnostics.

Metrics include registered capability count, running capability count, initialization duration, dependency failures, health failures, and recovery count.

Budgets include maximum capabilities, interfaces, dependencies, diagnostics, snapshots, registration target, initialization target, discovery target, and diagnostics overhead target.
