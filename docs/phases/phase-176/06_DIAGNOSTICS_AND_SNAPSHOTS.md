# Diagnostics And Snapshots

Provider: `presentationRuntime`

Diagnostics expose capability identity, active sessions, queue state, consumers, acknowledgements, synchronization, evidence, metrics, profiler records, budgets, governance, certification, and runtime posture.

Snapshots are isolated deep copies and include `presentationRuntimeSnapshot`.
