# Registries

Phase 166 includes dedicated registries for query definitions, query requesters, and query handlers.

Definitions require query type, namespace, schema version, owner runtime, payload validator, response validator, consistency model, compatibility policy, and allowed requesters.

Each query type resolves to one authoritative handler.
