# Baseline

Phase 166 introduces the Runtime Query Bus and Read-Only Access Foundation after Phase 164 Runtime Event Bus and Phase 165 Runtime Command Bus.

The Query Bus owns deterministic read-only information retrieval. It does not own commands, events, gameplay logic, AI, rendering, networking, persistence mutation, Workspace mutation, remotes, analytics, telemetry, or client authority.
