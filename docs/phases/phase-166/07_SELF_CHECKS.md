# Self-Checks

Phase 166 self-checks cover query definition registration, duplicate rejection, requester registration, handler registration, unknown query rejection, unknown requester rejection, invalid payload rejection, queueing, duplicate query id rejection, execution, batch queries, cancellation, diagnostics, snapshots, budgets, immutable results, immutable snapshots, shutdown rejection, and forbidden runtime surface absence.
