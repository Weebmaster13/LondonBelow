# Governance

The Runtime Query Bus Governance contract owns read-only query definitions, routing, authorization, projections, read models, cache policy metadata, diagnostics, snapshots, observability, budgets, compatibility, and maintenance.

It explicitly does not own gameplay mutation, command execution, event publication, networking, persistence mutation, Workspace mutation, analytics, telemetry, or client authority.
