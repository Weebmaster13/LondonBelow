# Diagnostics

Diagnostics expose `saveSessionRuntimePosture`, `activeSessions`, `dirtySessions`, `lockedSessions`, `transactions`, `recoveries`, `cancellations`, and `shutdowns`.

Diagnostics are health and lifecycle metadata only.
