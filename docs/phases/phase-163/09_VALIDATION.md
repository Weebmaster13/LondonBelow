# Validation

Validation covers session identity, save identity, provider identity, supported states, lifecycle transitions, duplicate sessions, nested transactions, stale locks, rollback without transaction, cancelled sessions, and recovery eligibility.

Validation happens before mutation.
