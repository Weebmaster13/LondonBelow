# Snapshots

Snapshots expose immutable `sessionSnapshot`, `lifecycleSnapshot`, `transactionSnapshot`, `lockSnapshot`, and `recoverySnapshot`.

Snapshots are isolated copies and are not gameplay state.
