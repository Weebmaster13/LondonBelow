# Presentation Contracts

Contract identity: `dialoguePresentationContract`

Provider identity: `dialogueRuntimePresentationContract`

Contracts define supported presentation kinds, acknowledgement kinds, synchronization policies, provider names, diagnostics provider, snapshot provider, phase, and certification posture.

The default contract is registered in memory during runtime reset and initialization.
