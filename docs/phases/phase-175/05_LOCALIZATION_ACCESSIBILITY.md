# Localization And Accessibility

Localization references are stable tokens. Dialogue does not resolve languages, translate strings, format pluralization, select fonts, or render localized content.

Accessibility metadata expresses intent through tokens, reading order, screen reader label references, pacing hints, emphasis metadata, and related records.

The registries return immutable copies and remain isolated per presentation request.
