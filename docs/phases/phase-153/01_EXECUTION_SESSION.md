# Execution Session

The Phase 153 harness is `automation/runtime-execution/Phase153RuntimeBootstrapValidation.mjs`.

It calls `runRuntimeExecution` with:

- phase: `153`
- phase name: `Chapter 0 Runtime Execution & Bootstrap Validation`
- requested backend: `StudioManual`
- runtime evidence required: `true`

The session is created by the Runtime Execution Framework. It is not created by a separate launch pipeline.
