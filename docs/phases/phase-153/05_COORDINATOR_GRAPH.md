# Coordinator Graph

Phase 153 reports a runtime dependency graph for the intended bootstrap path:

Runtime Execution Framework -> Studio Backend -> Runner -> Bootstrap -> Core -> Governance -> Contracts -> Diagnostics -> Snapshots -> Observation -> Interaction -> Presentation -> Chapter0

Nodes without imported runtime evidence are marked `BLOCKED` and `NOT_EXECUTED`.
