# Snapshots

Framework snapshots are represented by the execution session, manifest, timeline, bootstrap subsystem report, coordinator graph, and runtime scorecard.

Roblox runtime snapshots remain blocked until the manual backend imports a matching structured result.
