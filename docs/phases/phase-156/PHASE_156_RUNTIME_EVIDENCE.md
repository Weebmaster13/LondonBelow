# Runtime Evidence

The runtime records bounded evidence for eligibility, authorization, handler, execution, and cancellation stages.

Automation command `npm run london:interaction-runtime` uses the Runtime Execution Framework and reports blocked truthfully when Roblox Studio runtime evidence cannot be imported.
