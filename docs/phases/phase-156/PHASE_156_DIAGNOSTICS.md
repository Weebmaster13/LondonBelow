# Diagnostics

Diagnostics expose health-only lowerCamelCase posture through `interactionRuntimePosture`, plus counts for interactions, targets, sessions, evidence, contention, validation failures, and bounded limits.

Diagnostics do not mutate runtime state or certify gameplay execution.
