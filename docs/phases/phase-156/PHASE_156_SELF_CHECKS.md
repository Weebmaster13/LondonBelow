# Self-Checks

Run:

```powershell
npm run london:phase156:selfcheck
```

Coverage includes provider/API presence, target validation, request validation, duplicate request rejection, rate limiting, cancellation, handler failure, lowerCamelCase diagnostics posture, docs presence, and Phase 155 upstream regression self-checks.
