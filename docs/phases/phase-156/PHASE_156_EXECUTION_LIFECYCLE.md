# Execution Lifecycle

Lifecycle states are `Planned`, `Authorized`, `Executing`, `Completed`, `Rejected`, `Cancelled`, and `Failed`.

The default runtime path creates a plan and evidence without applying gameplay. Optional handlers must satisfy the explicit handler contract.
