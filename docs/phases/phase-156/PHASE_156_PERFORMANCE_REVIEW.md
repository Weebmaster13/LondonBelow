# Performance Review

All runtime histories are bounded. Request rate windows are trimmed during evaluation. Snapshot and evidence histories are bounded.

The runtime does not add per-frame server loops.
