# Target Identity

Targets use stable `targetId` values owned by server systems. Target records include `ownerSystem`, `targetStatus`, `adapterKind`, and metadata. Duplicate target ids reject before mutation.

Runtime targets are schemas, not Workspace object mutation authority.
