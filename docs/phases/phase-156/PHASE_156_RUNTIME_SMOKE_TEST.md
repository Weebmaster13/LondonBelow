# Runtime Smoke Test

Run:

```powershell
npm run london:interaction-runtime
```

The command invokes the Runtime Execution Framework for Phase 156. If Studio evidence is unavailable, the result is blocked, not certified.
