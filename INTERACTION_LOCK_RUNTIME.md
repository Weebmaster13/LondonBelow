# Interaction Lock Runtime

`InteractionLockRuntime` records interaction lock schemas.

Locks are coordination data only. They are not physics locks, door locks, inventory locks, or Workspace constraints.
