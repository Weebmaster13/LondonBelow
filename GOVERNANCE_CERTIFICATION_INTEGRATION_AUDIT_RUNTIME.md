# Governance Certification Integration Audit Schema

`GovernanceCertificationIntegrationAudit` records copied audit metadata for the certification integration coordinator.

Audit records are metadata-only and cannot repair, authorize, execute, schedule, orchestrate, persist, or mutate state.
