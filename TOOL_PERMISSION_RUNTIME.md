# Tool Permission Runtime

Permission schemas describe future access boundaries without granting any powers.

The runtime records permission data only. It does not authenticate users, authorize commands, moderate players, expose admin tools, or create remote consoles.

Malformed permissions and duplicate permission ids reject before state changes.
