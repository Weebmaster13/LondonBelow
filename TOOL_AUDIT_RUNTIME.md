# Tool Audit Runtime

Audit records describe future developer tooling audit events as inert data.

They are not moderation logs, analytics collection, remote console logs, DataStore writes, or live admin records.

Audit records are useful for designing future accountability, but they do not imply that any tooling action has happened.
