# World Connection Runtime

Connection schemas describe possible relationships between world ids. They include `connectionId`, `fromWorldId`, `toWorldId`, `connectionType`, `traversalKind`, `requiredSchemas`, metadata, context, and tags.

Connections do not execute traversal, open doors, teleport players, move characters, request pathfinding, or mutate physical objects.
