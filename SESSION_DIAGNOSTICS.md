# Session Diagnostics

Session diagnostics expose lifecycle state, session count, player session count, party count, readiness count, lifecycle count, join/leave count, per-category limit usage, sanitized validation failures, snapshot count, runtime limits, serialization posture, snapshot isolation proof, no-execution posture, last self-check result, and health state.

Diagnostics are read-only copies and safe for Framework health checks.
