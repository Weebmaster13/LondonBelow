# Session Diagnostics

Session diagnostics expose lifecycle state, session count, player session count, party count, readiness count, validation failures, snapshot count, runtime limits, serialization posture, snapshot isolation proof, no-execution posture, last self-check result, and health state.

Diagnostics are read-only copies and safe for Framework health checks.
