# Objective Definition Runtime

Objective definitions contain `objectiveId`, `objectiveType`, `ownerSystem`, `tasks`, `requirements`, `dependencies`, `state`, metadata, context, and tags.

Definitions are structural records only. They do not mark objectives complete, start quests, unlock content, write story, or trigger effects.
