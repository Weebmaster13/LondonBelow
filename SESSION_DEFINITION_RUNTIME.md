# Session Definition Runtime

Session definitions describe stable server-owned session identity. They include `sessionId`, `sessionType`, `ownerSystem`, metadata, context, and tags.

Definitions do not start matchmaking, reserve servers, teleport players, spawn players, or run gameplay.
