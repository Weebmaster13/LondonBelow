# Persistence Policy Runtime

Write and retry policy schemas describe future persistence constraints.

They do not call DataStore APIs, retry failures, schedule writes, or grant client save authority.
