# World Streaming Runtime

Streaming region schemas describe future streaming intent. They include `streamingRegionId`, `worldIds`, `streamingPolicy`, `priority`, metadata, context, and tags.

This runtime does not stream rooms, load maps, unload maps, read terrain, create terrain, or modify Workspace. Supported policies are conservative schema labels only.
