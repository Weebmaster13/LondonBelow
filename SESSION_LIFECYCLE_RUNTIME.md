# Session Lifecycle Runtime

Lifecycle schemas and join/leave records describe session lifecycle data.

They do not execute joins, leaves, teleports, matchmaking, spawning, save persistence, or UI updates.
