# Analytics Metric Runtime

Metric definitions describe future metrics without calculating or collecting them.

Each metric record is schema-only and must stay free of live profiling, tracking, telemetry transport, DataStore writes, remotes, and client authority.
