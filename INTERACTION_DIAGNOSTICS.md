# Interaction Diagnostics

Interaction diagnostics expose initialized, started, lifecycle state, interaction count, eligibility count, intent count, lock count, cooldown count, validation failure count, recent sanitized validation failures, snapshot count, runtime limits, serialization posture, snapshot isolation proof, last self-check result, and health state.

Diagnostics are read-only returned copies.
