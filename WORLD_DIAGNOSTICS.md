# World Diagnostics

World diagnostics expose lifecycle state, counts for each schema category, validation failures, snapshot counts, runtime limits, serialization posture, snapshot isolation posture, last self-check result, and health state.

Diagnostics are read-only copies. They are safe for Framework health checks and future developer tooling.
