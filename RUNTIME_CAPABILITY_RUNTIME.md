# Runtime Capability Runtime

Capabilities are declarations, not granted execution permissions.

Capability records require an existing runtime node and a stable capability name. They do not expose runtime objects, service references, callbacks, remotes, or executable permissions.
