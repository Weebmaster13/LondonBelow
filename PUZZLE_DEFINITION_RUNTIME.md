# Puzzle Definition Runtime

`PuzzleDefinitionRuntime` stores puzzle schemas and bounded progress records.

Every puzzle schema includes `puzzleId`, `puzzleType`, `ownerSystem`, `graph`, `nodes`, `edges`, `conditions`, `dependencies`, `metadata`, `context`, and `tags`.

Definitions are schema records only and do not execute puzzle behavior.
