# Governance Certification Integration Schema

`GovernanceCertificationIntegration` is the top-level metadata record for Asset Governance Certification Integration coordination.

It owns copied certification coordination metadata only. It does not authorize execution, inspect live runtimes, repair records, mutate upstream systems, schedule work, orchestrate systems, or persist data.
