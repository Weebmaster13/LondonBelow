# Runtime Gate Block Runtime

`RuntimeGateBlock` records block metadata attached to a runtime gate.

Fields:

- `blockId`
- `gateId`
- `blockKind`
- `severity`
- `active`
- `reason`
- `tags`
- `metadata`

Blocks are ledger records only. They do not unload, disable, mutate, or remove assets.
