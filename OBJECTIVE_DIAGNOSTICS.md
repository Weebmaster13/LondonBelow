# Objective Diagnostics

Objective diagnostics expose lifecycle state, objective count, task count, requirement count, dependency count, progress count, validation failures, snapshot count, runtime limits, serialization posture, snapshot isolation proof, no-execution posture, last self-check result, and health state.

Diagnostics are read-only copies and safe for Framework health checks.
