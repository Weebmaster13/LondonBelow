# Runtime Gate Check Runtime

`RuntimeGateCheck` records check metadata attached to a runtime gate.

Fields:

- `checkId`
- `gateId`
- `checkKind`
- `required`
- `passed`
- `summary`
- `tags`
- `metadata`

Checks are evidence metadata only. They do not evaluate runtime behavior or mutate assets.
