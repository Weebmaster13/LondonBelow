# Governance Certification Integration Chain Schema

`GovernanceCertificationIntegrationChain` records the copied certified governance chain order, provider list, and readiness ids.

The chain is static metadata. It never stores live module references or runtime handles.
