# Asset Approval Ledger Diagnostics

Diagnostics are health-only. They expose lifecycle state, counts, limit usage, validation failure count, snapshot count, approval posture, no-loading posture, no-execution posture, recent sanitized validation failures, and the last self-check result.

Diagnostics do not expose services, asset handles, loaded assets, runtime handles, remotes, Workspace references, or execution adapters.
