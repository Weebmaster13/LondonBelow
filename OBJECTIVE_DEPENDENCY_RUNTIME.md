# Objective Dependency Runtime

Dependency schemas describe relationships between objective records or future schema ids.

Dependencies are data only. They do not unlock objectives, advance stories, or execute quests.
