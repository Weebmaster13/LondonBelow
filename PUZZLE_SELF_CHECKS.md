# Puzzle Self-Checks

`PuzzleSelfChecks` certifies malformed puzzle rejection, duplicate puzzle rejection, unsupported type rejection, malformed graph rejection, duplicate node rejection, invalid node rejection, duplicate edge rejection, invalid edge rejection, invalid dependency rejection, duplicate dependency rejection, invalid condition rejection, duplicate condition rejection, valid puzzle registration, valid progress recording, malformed progress rejection, unknown puzzle progress rejection, unsafe progress rejection, unsafe tag rejection, graph/node/dependency/condition limit rejection, client/remote rejection, Workspace/Instance rejection, gameplay/interaction/inventory execution rejection, MonsterAI/Narrative/Save/Horror/Chapter/story/dialogue/cutscene rejection, serialization cycle and unsafe runtime rejection, oversized/deep payload rejection, snapshot isolation, diagnostics read-only behavior, bounded histories, and shutdown cleanup.
