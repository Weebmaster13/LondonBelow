# Runtime Group Runtime

Groups are classification records, not execution groups.

Groups may reference only registered runtime nodes. Group node counts are bounded. Groups do not start, stop, load, initialize, inject, orchestrate, or execute grouped runtimes.

Certified groups reject execution group payloads, lifecycle execution payloads, orchestration execution payloads, service references, remotes, and client authority.
